const producer = {
  gameProperty: res => {
    console.log('RE', res);
    // let result = {
    //   unstart: [],
    
    // };
    // const parent = res;

    // let item;
  
 
    // parent.forEach(element => {
    
    //   result.unstart.push(element);
    // });


    return result;
  },
};

export default {
  producer,
};